/**
 * TODO
 * - History of versioning software 
 * - Roles of Git vs Github 
 * - Command Commands of git
 * - gitignore
 * - commit formatting tool (commitzen)
 * 
 * == Git Commands ==  
 * One time commands
 *    - init 
 *    - remote (depends on setup)
 *    -- add 
 *    -- remove
 *    -- set-url
 * 
 * Work flow commands
 *      - add -> two things -- add to the next commit (staged) + init tracking
 *      - commit -> creates a point in the timeline
 *      - push 
 *      - pull 
 *      - branch 
 *      - checkout 
 *      - status
 *      - log
 * 
 * Practice Peer Workflow with Forks
 *    - fork 
 *    - set upstream 
 * -----------------------------------
 */

console.log("hello");

console.log("No I am important");
